import type { AvatarStyleDefinition } from './styles.js';
import type { DirectionFramePlan } from './types.js';
const continuity = 'Keep the same identity, hairstyle, clothing, proportions, camera distance, crop, subject scale, background, and lighting. Keep the shoulders almost front-facing. This is one frame in a mouse-following avatar set.';
export function centerPrompt(style: AvatarStyleDefinition): string { return `${style.prompt} Create a neutral, centered, front-facing head-and-shoulders avatar. ${continuity} Avoid: ${style.negativePrompt}`; }
export function directionPrompt(style: AvatarStyleDefinition, frame: DirectionFramePlan): string { return `Edit this canonical avatar into the ${frame.label} directional frame. Shift the head and gaze subtly toward screen angle ${frame.angle} degrees, where right is 0 degrees, down is 90, left is 180, and up is 270. Use only a gentle pose change suitable for interpolation between nearby static frames. ${continuity} Preserve the exact visual style of the reference. Avoid: ${style.negativePrompt}`; }
